import UIKit

enum ProgressBar {
    enum information {
        struct Request {
            var clockType: ClockInfo?
        }
        struct Response {}
        struct ViewModel {
            var checkType: String
        }
    }
}
